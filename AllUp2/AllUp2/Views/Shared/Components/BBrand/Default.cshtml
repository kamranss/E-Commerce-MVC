@model List<Brand>

@foreach (var brand in Model)
{
    <div class="col-lg-2">
        <div class="single-brand">
            <a href="@brand.WebSite">
                <img src="~/assets/images/brand/@brand.ImageURL" alt="brand">
            </a>
        </div> <!-- single brand -->
    </div>
}